// Deleting Files & Folders

// rm = remove files
// rm index.html
// rm index.html app.js style.css


// rmdir = remove directory command, removes empty folders
// rmdir FolderName

// rm -rf = (rf = recursive force, remember method => r = recursive delete, f = forced delete)removes any folders
// rm -rf FolderName


// after deleting form terminal it cant be restored