// Local v/s Global

// local - packages dosnt work outside the installed directory (is the best practce)

// it is not advaisable to install packages globall

// npm install -g <- package name -> // to install globally in path C:\Users\imam0\AppData\Roaming\npm\node_modules

// npm link <- package name -> // after instlling package in global level you have to link it first